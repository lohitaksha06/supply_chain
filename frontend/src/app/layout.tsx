import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/contexts/AuthContext";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PharmaChain - Blockchain Pharmaceutical Supply Chain",
  description: "Secure pharmaceutical supply chain tracking with blockchain technology. Verify medicine authenticity and track batches from manufacturer to patient.",
  keywords: ["pharmaceutical", "blockchain", "supply chain", "medicine verification", "healthcare"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
